# 3.2 Реалізація Query Builder (DML AST)

<!-- Що описати:
  - QueryBuilder як fluent-фасад
  - Типи IR: SelectQuery, InsertQuery, UpdateQuery, DeleteQuery
  - Принцип: жодного SQL не емітується тут — лише AST
  - Приклади побудови (фрагмент коду): select with where, join, order by
  - Тестування через snapshot AST
-->
