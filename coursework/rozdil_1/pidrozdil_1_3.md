# 1.3 Типобезпека в ORM та межі статичної верифікації

<!-- Що описати:
  - Що означає "типобезпека" в контексті ORM (типізовані WHERE-оператори, return types, include-конфігурації)
  - Generic-параметризація запитів у TypeScript
  - Межі: те, що не можна верифікувати статично (стан бази, raw SQL, runtime-mapping)
  - Підходи: schema-first (codegen), декоратори, query-first
  - Trade-off: гнучкість vs строгість типів
-->
