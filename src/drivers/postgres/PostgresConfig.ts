// import {DriverConfig} from "@/drivers/common/DriverConfig";

// import {DriverConfig} from "drivers/common/DriverConfig";

import {DriverConfig} from "../common/DriverConfig";

/**
 * Configuration interface for PostgreSQL driver.
 *
 * Inherits all standard configuration options from `DriverConfig`.
 */
export interface PostgresConfig extends DriverConfig {

}